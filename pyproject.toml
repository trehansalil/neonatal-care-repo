[project]
name = "neonatal-management"
version = "0.1.0"
description = "Neonatal care management application"
requires-python = ">=3.11"
dependencies = [
    "Flask>=3.0.0",
    "Flask-CORS>=4.0.0",
    "clickhouse-connect>=0.7.0",
    "pytz>=2024.1",
    "Werkzeug>=3.0.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
only-include = ["app.py", "html/*"]
